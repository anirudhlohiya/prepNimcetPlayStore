<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PrepNimcet" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/color5</item>
        <item name="colorPrimaryVariant">@color/color5</item>
        <item name="android:statusBarColor">@color/action_bar</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PrepNimcet" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/color5</item>
        <item name="colorPrimaryVariant">@color/color5</item>
        <item name="android:statusBarColor">@color/action_bar</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Base.Theme.PrepNimcet" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Define your custom shape appearance -->
    <style name="ShapeAppearance.SmallComponent" parent="ShapeAppearance.Material3.SmallComponent">
        <!-- Customize corner size -->
        <item name="cornerSize">8dp</item> <!-- Adjust the corner size as needed -->
        <!-- Customize elevation -->
        <item name="elevationOverlayEnabled">true</item> <!-- Enable elevation overlay -->
        <item name="elevation">8dp</item> <!-- Adjust the elevation as needed -->
    </style>

</resources>